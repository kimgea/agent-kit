# Tools

Purpose: provider-neutral tool definitions and contracts.

Place here:
- tool manifests
- invocation contracts
- constraints and expected outputs

Do not place here:
- tool implementations owned by an external platform
- provider adapter templates

Real asset path:

```text
tools/<tool-id>/
  manifest.yaml
  ...
```
