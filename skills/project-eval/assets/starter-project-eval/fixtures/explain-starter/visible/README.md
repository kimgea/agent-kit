# Starter evaluation fixture

This isolated fixture exists to prove that the local evaluation machinery can
materialize a case and grade structured output. It does not establish coverage
of the adopting repository. Record that boundary in `answer.json` using the
allowed values in `answer.schema.json`.
