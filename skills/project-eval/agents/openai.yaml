interface:
  display_name: "Project Eval"
  short_description: "Set up and run local repository evals"
  default_prompt: "Use $project-eval to inspect this repository's evaluation readiness and preview a minimal setup without changing files or invoking a model."
