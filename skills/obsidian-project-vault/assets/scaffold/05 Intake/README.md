# Intake

Use this folder for material that exists before it has been adapted into the vault.

Subfolders:

- `To Process`
- `In Review`
- `Processed`
- `Rejected`

Rule:

- `05 Intake` is only for active staging
- once a batch is processed, move the raw files to `65 Sources/`
- durable outcomes belong elsewhere in the vault
