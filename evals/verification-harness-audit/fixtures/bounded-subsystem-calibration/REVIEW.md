# Required local verification workflow

Unit tests, static analysis, and the local CI configuration are coherent audit subsystems. The required routine command must select the existing schema compatibility test exactly once. Overlapping reports about that one omission are one root cause.
