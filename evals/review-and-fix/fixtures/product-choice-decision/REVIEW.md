# Review policy

The interface density must be either `compact` or `spacious`; `legacy` is not
supported and a touched configuration using it must not ship. Both supported
values are valid product choices. Reviewers and fixers must not select between
them without an explicit product-owner decision.
