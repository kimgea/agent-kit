# Verification policy

Required for `docs/help.md`: establish that the instructions work on a fully
offline device. `python -B tests/check_format.py` checks Markdown formatting but
does not establish offline behavior.
