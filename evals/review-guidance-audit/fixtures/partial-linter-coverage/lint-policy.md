# SQL linter

The required fast local linter deterministically rejects direct SQL string concatenation with an actionable source location. It cannot prove authentication, tenant identity, or transaction semantics.
