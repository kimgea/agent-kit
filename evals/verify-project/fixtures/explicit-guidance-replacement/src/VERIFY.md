# Verification

For targets under `src/`, replace the exact root requirement to run
`python tests/check_all.py` with `python tests/check_fast.py`. The focused check
fully covers this isolated module and is the required check for this subtree.
