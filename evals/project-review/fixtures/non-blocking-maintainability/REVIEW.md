# Formatter review policy

When a formatter repeats the module's shared title constant as a literal,
report a non-blocking maintainability suggestion to reuse the constant. This
duplication is not a must-not-ship defect.
