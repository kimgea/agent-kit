---
name: Add the fixed Codex runner and measurement
status: closed
created: 2026-09-08T13:34:04Z
updated: 2026-09-08T19:25:00Z
github: (will be set on sync)
depends_on: [001, 002]
parallel: false
conflicts_with: []
---

# Task: Add the fixed Codex runner and measurement

## Description

Add one reviewed Codex execution adapter with frozen configuration, cumulative
budgets, process containment, and provenance-rich measurement. Preserve an
agent-neutral recorded-output path for every other integration.

## Acceptance Criteria

- [x] Codex discovery, version, argv, environment, model, reasoning setting,
  skills/instructions, and relevant configuration are frozen and digested.
- [x] The runner uses a fresh context and only the sanitized case workspace.
- [x] Host-owned manifests, graders, results, event/error capture, credentials,
  and holdouts remain outside agent-writable and agent-readable paths.
- [x] `smoke`, `compare`, and `confidence` profiles enforce cumulative cases,
  repetitions, invocations, timeout, token/cost when observable, writes,
  network, and effects; retries consume the same budget.
- [x] Timeouts terminate and reap the complete process tree before mutation
  checks continue.
- [x] Measurements identify `host_observed`, `runner_reported`, or
  `unavailable` provenance.
- [x] Offline/hermetic execution is the default; networked execution requires a
  distinct explicitly authorized profile.
- [x] Recorded-output grading works without Codex and does not imply Claude or
  other direct-runner compatibility.
- [x] No canonical check, ordinary CI, commit, push, release, or installation
  path invokes a model.

## Technical Details

- Reuse proven containment concepts from the repository behavioral harness
  without importing repository code at installed runtime.
- Test Windows and POSIX process-tree, path, temp, launcher, link/reparse, and
  result-capture boundaries.
- Retain only bounded sanitized error categories and digests after execution.

## Dependencies

- Tasks 001 and 002.
- Local Codex CLI for explicit forward evidence only.

## Effort Estimate

- Size: XL
- Hours: 9-12

## Definition of Done

- [x] Adapter and recorded-output path implemented.
- [x] Containment and budget tests pass cross-platform.
- [x] At least one explicit fresh Codex case produces canonical evidence.
- [x] Code reviewed.

## Delivery Evidence

- Focused deterministic runner and protocol tests: 57 passed, 2 intentional
  platform skips. Ordinary tests cannot invoke a model.
- Canonical repository gate: 587 passed, 9 intentional platform
  skips; no model was invoked by the gate.
- Explicit offline Codex 0.150.1 smoke on `gpt-5.6-sol` with medium reasoning:
  deterministic grade passed; attempt
  `294257110a76d5a5d5e5dd85c5c3c59731f96936a99cf054454a38ac391bad0c`;
  configuration
  `709e1f9647644bd7b1ffcea5b4bd2234452764c3a57fc2d2fac8f7a1beee9dab`.
- Token and cost measurements were unavailable in that runner event stream and
  were recorded as unavailable rather than inferred.
- Trusted `project-review` review: PASS with 9/9 paths covered and no findings
  or material limitations. Three attempted independent review contexts did not
  return before their bounded deadlines, so this task used the same canonical
  review method locally; the final pull request still requires an independent
  exact-head review.
