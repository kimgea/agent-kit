---
name: Integrate catalog packaging docs and forward tests
status: closed
created: 2026-08-29T09:45:52Z
updated: 2026-08-29T13:58:16Z
github: null
depends_on: [005]
parallel: false
conflicts_with: []
---

# Task: Integrate catalog packaging docs and forward tests

## Description

Register the validated skill and single-skill plugin, complete public and
maintainer documentation, verify generated packages, and forward-test the skill
on representative repositories before delivery.

## Acceptance Criteria

- [x] `toolkit.toml` is the source of truth for a `project-review` resource and
      plugin with Codex/Claude and Linux/Windows/macOS compatibility metadata.
- [x] Architecture, compatibility, README/catalog surfaces, docs, tests, and eval
      paths agree with the shipped interface and maturity.
- [x] The skill package and plugin contain every runtime script/reference and no
      repository-only implementation dependency.
- [x] A sample `REVIEW.md` in docs teaches consequential checks, scope, safe paths,
      recommended verification, and noise avoidance without becoming runtime
      maintainer history.
- [x] Forward tests cover a small single-agent review, a multi-chain review,
      structured JSON consumption, and delegated review where agents are available.
- [x] The final diff is independently reviewed, generated artifacts are current,
      and `python scripts/agent_kit.py check` passes without changing the worktree.

## Technical Details

Use a single-skill plugin rather than adding the resource to the artifacts plugin.
Do not add GitHub publishing or fixing behavior. Mark maturity conservatively
until forward tests demonstrate stable results.

## Dependencies

Task 005.

## Effort Estimate

- Size: M
- Hours: 2

## Definition of Done

- [x] Catalog, archives, plugin manifest, and marketplace outputs are consistent
- [x] Canonical validation and package checks pass on a clean tree
- [x] Review evidence and known limitations are recorded for handoff

## Completion Evidence

- `python scripts/agent_kit.py check`: 129 tests passed, one intentional skip.
- Standalone skill, focused plugin, and Agent Kit marketplace archives generated
  at toolkit version 1.4.0; the extracted standalone skill passed metadata and
  resolver smoke tests without repository-only imports.
- Fresh-agent evaluations covered a safe counterexample, trusted-base rule
  self-change, nested tenant isolation, missing link-like guidance, caller and
  repository command authority, an ordinary cross-file unit bug, malicious
  guidance, and a rename governed by distinct source and destination chains.
- Independent reviews found and drove fixes for ten distinct blockers. The final
  corrective-delta review returned `PASS` with zero findings; canonical helper
  validation succeeded. Remaining limitation: native hosted Windows/macOS proof
  is deferred to delivery CI, while unit fixtures exercise portable path and
  Windows reparse behavior locally.
