---
name: Implement current-state target and VERIFY guidance resolver
status: closed
created: 2026-09-05T19:50:10Z
updated: 2026-09-05T21:58:52Z
github: (will be set on sync)
depends_on: [001]
parallel: true
conflicts_with: [005]
---

# Task: Implement current-state target and VERIFY guidance resolver

## Description

Implement the self-contained resolver for combined working-tree and explicit
current paths, bounded project context, trusted root-to-nearest `VERIFY.md`
chains, caller intent, freshness, caps, and command-authority provenance.

## Acceptance Criteria

- [x] Resolve combined Git working-tree and explicit file, directory, and project
      targets against the current filesystem.
- [x] Represent additions, modifications, deletions, renames, modes, and visible
      untracked paths without confusing absent and unread content.
- [x] Resolve independent root-to-nearest `VERIFY.md` chains for every target and
      read committed `HEAD` guidance in Git repositories.
- [x] Represent additive guidance, explicit scoped replacements, ambiguous
      conflicts, and non-Git current-file provenance.
- [x] Inventory bounded related manifests, test/build/lint/type configuration,
      scripts, documentation, and local CI metadata without expanding scope.
- [x] Freeze caller intent, run caps, active-policy provenance, verifier freshness,
      and candidate command metadata without granting execution authority.
- [x] Reject path escapes, controls, links/reparse points, aliases, special files,
      ignored broad-discovery paths, races, unreadable content, and exhausted
      budgets with material limitations.

## Technical Details

- Keep resolver code inside `skills/verify-project/scripts/`.
- Reuse behavioral hierarchy semantics through conformance fixtures, not runtime
  imports from another skill or repository script.
- Bind reads to opened identities and normalize text provenance across platforms.
- Make output deterministic for identical current state and invocation context.

## Dependencies

- Task 001 canonical contracts.

## Effort Estimate

- Size: L
- Hours: 8

## Definition of Done

- [x] Resolver implementation complete.
- [x] Focused resolver and security-boundary tests pass.
- [x] Output validates against the frozen context contract.
